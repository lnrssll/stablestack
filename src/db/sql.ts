// Raw SQL query strings. Import into queries.ts to build typed wrappers.
// One constant per query; name in SCREAMING_SNAKE_CASE matches the function name in queries.ts.
//
// Example:
// export const LIST_POSTS = `
//   SELECT id, title, created_at FROM posts ORDER BY created_at DESC
// `
